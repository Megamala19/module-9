# fetchuserapp

React Hands-On Lab: Consuming REST APIs.

A `Getuser` component that calls `https://api.randomuser.me/` inside
`componentDidMount()` using `fetch`, and renders the returned user's
title, first name, and picture.

## Run it

```bash
npm install
npm start
```
